<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.shop.dao.ProductDAO">
  
  <insert id="productInfo" parameterType="Product">
  	INSERT INTO product
  		(p_id, c_id, p_name, p_text, p_title, p_price, p_quantity) 
  	VALUES (#{p_id},#{c_id},#{p_name},#{p_text},#{p_title},#{p_price},#{p_quantity})
  </insert>

</mapper>
