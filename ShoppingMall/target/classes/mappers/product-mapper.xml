<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.shop.dao.ProductDAO">
  
	<!-- 상품등록 Add -->
	<insert id="productAdd" parameterType="Product">
		INSERT INTO product
			(p_id, c_id, p_name, p_text, p_title, p_price, p_quantity, u_id) 
		VALUES (#{p_id},#{c_id},#{p_name},#{p_text},#{p_title},#{p_price},#{p_quantity},#{u_id})
	</insert>

	<!-- 상품페이지 Read -->
	<select id="productRead" parameterType="Product">
		SELECT * FROM product WHERE p_id = #{p_id} and u_id = #{u_id} and c_id = #{c_id}
	</select>
</mapper>






  <!-- 상품목록 가져오기 -->
<!--    <select id="productlist" parameterType="Product"> -->
<!--    	SELECT * FROM product; -->
<!--    </select> -->